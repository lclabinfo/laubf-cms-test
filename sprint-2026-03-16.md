# Sprint Summary — Sunday, March 16, 2026

> 13 commits across 4 work streams. Major themes: **auth hardening**, **speaker migration**, **public website polish**, **builder planning**.

---

## 1. Authentication & Security (4 commits)

- **Google account linking** — Settings page lets users connect/disconnect Google SSO to existing credentials accounts
- **Password UX** — Reusable `<PasswordInput>` with show/hide toggle + `<PasswordChecklist>` strength indicator; used across signup, reset, accept-invite
- **OTP password reset** — Replaced magic-link reset with 6-digit code flow (`input-otp` component, verify-reset-code endpoint, 15-min expiry)
- **Duplicate email detection** — Signup checks for existing accounts and suggests login/reset instead of failing silently
- **Resend invites** — Admin can resend invite emails from Users table; row action + bulk action
- **Invite email guard** — Server-side check that accepting user's email matches the invite email (prevents invite link sharing)
- **Login auth-method detection** — `/auth/check-auth-method` tells login page whether to show password field or Google-only prompt
- **Auto-link toast** — Dashboard shows success toast after Google account linking completes
- **Security audit doc** — `docs/00_dev-notes/security-audit-2026-03-16.md`
- **Auth docs reorg** — Moved 10 auth-related dev-notes into `docs/08_user-journeys/`

## 2. Speaker → Person Migration (5 commits)

- **Schema migration** — Added `speakerId` (FK to Person) on Message and BibleStudy, migrated all existing Speaker references via SQL
- **DAL + API update** — All message/bible-study queries now join through Person instead of legacy Speaker table
- **Dropped Speaker table** — Removed model, DAL module, API routes, and all references
- **Frequent speakers API fix** — Returns correct IDs after migration
- **Messenger dropdown fixes** — Fresh queries (no stale cache), multi-word search working, removed custom name option
- **Cached speaker list** — Tag-based invalidation for speaker/person queries
- **Deploy script** — Messenger group cleanup for production data

## 3. Public Website Fixes (3 commits)

- **16 section component fixes** — Updated data fetching, null-safety, calendar rendering, message cards, footer, CTA buttons across action-card-grid, all-messages, campus-card-grid, custom-embed, custom-html, event-calendar, faq, location-detail, media-grid, ministry-schedule, quote-banner, recurring-meetings, spotlight-media, statement, upcoming-events
- **Latest Message spotlight** — Homepage spotlight section now pulls latest message dynamically
- **Font loading** — R2 CDN URLs, dynamic format detection (woff2/woff/ttf), removed redundant @font-face declarations
- **Deploy scripts** — New `apply-website-fixes-2026-03-16.mts`, updated existing scripts

## 4. Builder Planning (1 commit)

- **Builder review** — Full audit of current builder state (what works, what's broken)
- **Builder roadmap** — 4-phase plan: Phase 1 (this week) = make every section editable; Phase 2 = design panel + page templates; Phase 3 = launch-ready

---

## Demos to Show

### Auth Flow (end-to-end)

1. **New user signup** → Create account with email/password → verify password strength checklist → land on onboarding
2. **Google account linking** → Log in with credentials → Settings → "Connect Google Account" → complete OAuth → see success toast on dashboard
3. **Forgot password (OTP)** → Click "Forgot password" → enter email → receive 6-digit code → enter code → set new password
4. **Invite flow** → Admin sends invite from Users page → recipient clicks link → email guard validates → account created → onboarding
5. **Resend invite** → Admin opens Users table → row action "Resend invite" on pending user → confirm email sent
6. **Google-only login hint** → User who signed up via Google tries credentials login → sees "Sign in with Google" prompt instead of password field

### Speaker Migration

7. **Speaker dropdown** → Create/edit a message → speaker dropdown searches People (not legacy Speakers) → multi-word search → select → save
8. **Public website verify** → Open a message detail page → speaker name renders correctly from Person table

### Public Website

9. **Homepage spotlight** → Show latest message appearing automatically on homepage spotlight section
10. **Section rendering** → Walk through 3-4 previously broken sections (event calendar, FAQ, recurring meetings) now rendering correctly
11. **Font rendering** → Show custom fonts loading correctly from R2 CDN (no FOUT or fallback flicker)

### Builder (Walkthrough Only — No Live Demo Yet)

12. **Builder roadmap review** → Walk through the 4-phase plan, current state (70% shell), and this week's targets (Phase 1: all sections editable by Friday)
