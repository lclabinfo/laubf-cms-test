# Standup — March 16, 2026

## What We Shipped (Fri Mar 13 – Sun Mar 15)

### Friday, March 13

- **Favicons & titles**: Split favicons and page titles correctly between CMS and public website.
- **Archived members**: New dialog to view/restore archived members, plus legacy member import script and users table improvements.
- **Website event page**: Enhanced event detail page with better event actions (add-to-calendar, share, registration) and updated seed data.
- **R2 media storage overhaul**: Centralized upload/delete through `lib/upload-media.ts` — staging flow (upload to `staging/`, promote on save), TipTap rich text editor integration for inline image uploads. New API routes: `media/promote`, `media/by-url`, expanded `media/[id]/usage`. Orphaned R2 cleanup script. Follow-up fix for TipTap image rendering.
- **Auth & security**: Accept-invite flow fix (merged feature branch), account security settings page cleanup, password change endpoint hardened.
- **Website builder roadmap**: New planning doc for builder phases.

### Saturday, March 14

- **Bible study material upload fix**: Corrected upload handling in the message entry form.
- **QA auth checklist**: Added full auth testing checklist doc.

### Sunday, March 15

- **Scripture passage input**: Tilde (`~`) support for verse ranges, comma-separated ranges (e.g., `John 3:16, 18-20`), auto-confirm on blur, persist unformatted text, validation modal layout polish.
- **Message entry CRUD**: Full actions — archive, publish/unpublish, bulk operations (bulk archive, delete, publish), undo-delete with toast. Added `archivedAt` field to schema.
- **Events CRUD**: Bulk actions (archive, delete, publish/unpublish), cross-page selection fix so selecting-all works beyond the visible page.
- **Website fixes**: Mobile button clicks and anchor scroll working on website pages.
- **Deploy scripts**: Updated church ID resolution pattern across 7 scripts, new deploy data script.

---

## What Took More Iterations

### Scripture Passage Input (3 commits)
Started with tilde support and auto-confirm on blur, then needed a follow-up commit for comma-separated ranges and validation modal layout fixes. The bible reference parsing logic in `lib/bible-data.ts` grew by ~290 lines across the iterations — the combinatorial complexity of valid scripture formats (tildes, commas, ranges, multi-chapter spans) required incremental handling rather than getting it right in one pass.

### R2 Media Storage (2 commits)
The main overhaul was ~2,600 lines touching 28 files — a large surface area connecting uploads, TipTap, event forms, message forms, series images, and media library. Needed a follow-up fix for TipTap image rendering and R2 path handling. The staging/promote pattern and making it work consistently across all the different upload surfaces (forms, rich text editor, media library) was the main source of complexity.

### Events CRUD + Cross-Page Selection
The cross-page selection bug surfaced only after messages CRUD was done — the same pattern was copied to events and needed a fix in both places. The messages commit was ~1,100 lines, then events required another ~830 lines, plus backporting the selection fix to the messages toolbar.

### Deploy Scripts
Three small commits (seed script, seed update, script fix) to get the deploy data application right — the church ID resolution pattern needed to be corrected across 7 deploy scripts.
