# Standup — Monday, March 23, 2026

Covers 24 commits from Friday Mar 20 through Sunday Mar 22.

---

## Navigation Editor (6 commits)

Rewrote the nav editor's drag-and-drop system from nested sortable contexts to a single flat tree. Items now reparent across levels by dragging horizontally — no more prefix hacks or broken nesting.

- Cross-level reparenting: drag an item between dropdowns or depths, groupLabel updates automatically
- Section headers (column dividers) are interactive: rename, remove grouping via context menu
- "Add Item" and "Add Section" wired into dropdown three-dot menus
- Move To submenu for keyboard-friendly reparenting
- Circular parentId prevention

## Section Editors & Theme Tokens (6 commits)

- ALL_MESSAGES editor: full layout controls (columns, card spacing, items per page, element toggles), column lock with icon overlay
- Upcoming Events editor: filter controls (max events, recurring, past window) now fully wired to backend queries end-to-end
- Theme token migration on 5 section components (action-card-grid, cta-banner, directory-list, event-calendar, quote-banner)
- message-card + all-messages-client migrated to useSectionTheme() — Brand/Dark/Muted color schemes now work
- New EditorSection collapsible component for grouping editor controls
- Hero editor reorganized: desktop/mobile video labels with resolution hints, poster moved below, overlay controls added

## Color Schemes (2 commits)

- Added BRAND and MUTED color scheme enum values (migration included)
- Section wrapper and theme tokens updated to support all 5 schemes (Light, Dark, Accent, Brand, Muted)

## Builder UX Polish (6 commits)

- Section header: inline rename (replaced browser prompt()), hover:bg-muted state, larger menu button
- Data-driven section banner: compact redesign with CMS breadcrumb + inline "Manage Events" button
- Section label removed from drawer header (lives in Display Settings only)
- Media pickers: filename shown on hover overlay, video picker accepts resolution hint descriptions
- Hero banner: fixed text positioning and CTA button alignment for left/center/right layouts
- Presence heartbeat: console.warn on failure for debugging

## Builder Feedback System (2 commits)

- Full stack: POST/GET API, DAL module, feedback dialog, admin review page
- BuilderFeedback Prisma model + migration

## Fixes & Infra (2 commits)

- Seed idempotency: church upsert instead of create (prevents dupes on re-run)
- Nav/page editor QA pass
